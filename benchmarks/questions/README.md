# Benchmark questions

Seed and frozen benchmark questions belong here. Production benchmark questions are generated
deterministically from canonical source evidence under the frozen generation policy; clinicians
do not build, approve, or unblock the benchmark.

The executable, release-bound suite combines questions with their gold labels under
`../suites/`. `synthetic-v1.json` is plumbing coverage only and is not clinical benchmark
evidence.
